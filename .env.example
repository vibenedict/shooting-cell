# Copy this file to .env.local and fill in your deployed ShooterScore address.
# .env.local is gitignored — real values never land in GitHub. Set the same
# variable in your Vercel project settings for prod.
NEXT_PUBLIC_SHOOTER_SCORE_ADDRESS=
